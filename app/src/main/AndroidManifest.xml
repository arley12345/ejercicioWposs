<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ejerciciowposs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".VisualizarTransaccion"></activity>
        <activity android:name=".VisualizarSaldoCorres" />
        <activity android:name=".VisualizarSaldo" />
        <activity android:name=".ConsultaSaldo" />
        <activity android:name=".Deposito" />
        <activity android:name=".Retiro" />
        <activity android:name=".Transferencia" />
        <activity android:name=".CrearCuenta" />
        <activity android:name=".ConfirmacionPagoTargeta" />
        <activity android:name=".PagoConTarjeta" />
        <activity android:name=".MenuPrincipal" />
        <activity android:name=".AgregarUsuario" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>